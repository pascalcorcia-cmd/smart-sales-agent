{
  "name": "smart-sales-agent",
  "version": "1.0.0",
  "description": "Agent IA commercial multi-outils avec Claude — recherche web, analyse de fichiers, qualification BANT/MEDDPICC, account planning",
  "private": true,
  "scripts": {
    "install:backend": "cd backend && pip install -r requirements.txt",
    "install:frontend": "cd frontend && npm install",
    "install:all": "npm run install:backend && npm run install:frontend",
    "build": "cd frontend && npm run build && node ../scripts/copy-build.js",
    "start": "cd backend && uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "dev:backend": "cd backend && uvicorn app.main:app --reload --port 8000",
    "dev:frontend": "cd frontend && npm run dev",
    "setup": "npm run install:all && npm run build"
  },
  "keywords": ["sales", "ai-agent", "claude", "b2b", "prospection", "account-plan"],
  "license": "MIT"
}
